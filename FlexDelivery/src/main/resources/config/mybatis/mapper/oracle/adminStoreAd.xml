<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="config.mybatis.mapper.oracle.adminStoreAd">
	<select id="approvalList" resultType="memberVo">
		select storead_no, store_no, advertise_no, ad_validate, ad_pay_flag, storead_regdate
		from storead
		order by storead_no desc
	</select>
	
</mapper>