<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="config.mybatis.mapper.oracle.adminmediumcategory">
	<!-- <sql id="searchWhere">
		<where>
			<if test="searchKeyword != null and searchKeyword != ''">
				${searchCondition} like '%' || #{searchKeyword} || '%'
			</if>
		</where>	
	</sql> -->
	
	<insert id="insertMediumcategory" parameterType="adminMediumCategoryVo">
		<selectKey keyProperty="mCategoryNo" resultType="int" order="BEFORE">
			select MEDIUMCATEGORY_seq.nextval from dual 
		</selectKey>
		
		insert into MEDIUMCATEGORY(M_CATEGORY_NO, M_CATEGORY_NAME, L_CATEGORY_NO, WEATHER_NO)
		values(#{mCategoryNo} ,#{mCategoryName}, #{lCategoryNo}, #{weatherNo})
	</insert>
	
	<select id="selectAll" parameterType="adminMediumCategoryVo" resultType="adminMediumCategoryVo">
		select M_CATEGORY_NO, M_CATEGORY_NAME, L_CATEGORY_NO, WEATHER_NO from MEDIUMCATEGORY
		order by M_CATEGORY_NO desc
	</select>
	
	<select id="selectAll2" parameterType="adminMediumCategoryViewVo" resultType="adminMediumCategoryViewVo">
		select l.L_CATEGORY_NO, l.L_CATEGORY_NAME, count(m.L_CATEGORY_NO) as L_C_COUNT, m.M_CATEGORY_NO, m.M_CATEGORY_NAME,
			w.WEATHER_NO, w.WEATHER_NAME, count(m.WEATHER_NO) as W_COUNT
		from LARGECATEGORY l left join MEDIUMCATEGORY m
			on l.L_CATEGORY_NO=m.L_CATEGORY_NO
		left join WEATHER w
			on m.WEATHER_NO=w.WEATHER_NO
		group by l.L_CATEGORY_NO, l.L_CATEGORY_NAME, m.M_CATEGORY_NO, m.M_CATEGORY_NAME, w.WEATHER_NO, w.WEATHER_NAME
		order by m.M_CATEGORY_NO
	</select>
	 
	<!-- 
	
	<select id="selectTotalRecord" parameterType="searchVo"
		resultType="int">
		select count(*) from reBoard
		<include refid="searchWhere"></include>
	</select>
	-->
	
	<!-- <select id="selectByNo" parameterType="int" resultType="AdminLargeCategoryVo">
		select L_CATEGORY_NO, L_CATEGORY_NAME, L_CATEGORY_FILENAME from LARGECATEGORY where L_CATEGORY_NO=#{lCategoryNo}
	</select>
	
	<update id="updateLargecategory" parameterType="adminLargeCategoryVo">
		update LARGECATEGORY 
		set L_CATEGORY_NAME=#{lCategoryName},L_CATEGORY_FILENAME=#{lCategoryFilename}
		<if test="fileName!=null and fileName!=''">
			, filename=#{fileName}, originalFileName=#{originalFileName},
			fileSize=#{fileSize}
		</if>		
		where L_CATEGORY_NO=#{lCategoryNo}
	</update>
	
	<delete id="deleteLargecategory" parameterType="adminLargeCategoryVo">
		delete from LARGECATEGORY where L_CATEGORY_NO=#{lCategoryNo}
	</delete> -->
	
	<!--
	<parameterMap type="map" id="adminLargeCategoryDeleteParam">
		<parameter property="no" javaType="string" jdbcType="VARCHAR" mode="IN"/>	
	</parameterMap>
	
	<delete id="deleteLargecategory" parameterMap="adminLargeCategoryDeleteParam">
		call deleteLargecategory(?)
	</delete>
	
	<update id="updateDownCount" parameterType="int">
		update reboard
		set downcount=downcount+1
		where no=#{no}
	</update> -->
	
</mapper>